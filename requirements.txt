# requirements.txt - Streamlit Cloud 배포용

# -------------------------------------------------
# 기본 프레임워크 및 데이터 처리
# -------------------------------------------------
streamlit
pandas
numpy

# -------------------------------------------------
# AI 및 API 관련
# -------------------------------------------------
google-generativeai

# -------------------------------------------------
# 날짜 및 시간 처리
# -------------------------------------------------
python-dateutil
pytz

# -------------------------------------------------
# 로깅 (Streamlit Cloud에서는 기본 로깅으로도 충분하지만,
# 고급 로깅이 필요할 경우 포함)
# -------------------------------------------------
loguru

# -------------------------------------------------
# 보안 (Streamlit이 내부적으로 사용하므로,
# 명시적으로 포함하면 버전 충돌을 방지하는 데 도움이 될 수 있음)
# -------------------------------------------------
cryptography

# -------------------------------------------------
# 설치 불필요 또는 환경 의존적인 패키지 (제거됨)
# -------------------------------------------------
# sqlite3: 파이썬 표준 라이브러리이므로 설치 대상이 아님 (오류의 주 원인)
# python-dotenv: Streamlit Cloud에서는 st.secrets를 사용하므로 불필요
