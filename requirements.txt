# requirements.txt - Streamlit Cloud 배포용

# -------------------------------------------------
# 기본 프레임워크 및 데이터 처리
# ------------------------------------------------겠습니다. 이번 `ModuleNotFoundError` 역시 이전 `matplotlib` 오류와 원인이 같습니다-
streamlit
pandas
numpy
tabulate

# -------------------------------------------------
# 시각화 (차트 생성에 필요)
# -------------------------------------------------
matplotlib

# -------------------------------------------------
# 데이터 직렬화 (YAML 파일 처리에 필요)
# -------------------------------------------------
PyYAML  # <--- 이 라인을 추가했습니다.

# -------------------------------------------------
# AI 및 API 관련
# -------------------------------------------------
google-generativeai

# -------------------------------------------------
# 날짜 및 시간 처리
# -------------------------------------------------
. 우리가 YAML 파일을 처리하기 위해 `import yaml` 코드를 추가했지만, 정작 이 기능을 제공하는 라이브러리를 설치하라고 알려주지 않았네요.

### **오류 분석**

*   **오류 종류:** `ModuleNotFoundError`
*   **Traceback:**
    *   `File ".../pages/0_MCP_관리.py", line 4, in <module>`
    *   `import yaml`
*   **핵심 의미:** `yaml`이라는python-dateutil
pytz

# -------------------------------------------------
# 로깅
# -------------------------------------------------
loguru

# -------------------------------------------------
# 보안
# -------------------------------------------------
cryptography
